const age = 96;
function calculateKrAge(ageOfForeigner) {
  ageOfForeigner + 2;
}

const calculator = {
  plus: function (a, b) {
    return a + b;
  },
};

const plusResult = calculator.plus(2, 3);

console.log(plusResult);
